# tests/

C++ unit/regression tests land here in Phase 1 onward — numerical parity checks
(`lab/`'s Python-vs-C++ diff harness, PRD §7), label-normalisation regression tests
(PRD §5), content-type router tests. Empty until Phase 1.
