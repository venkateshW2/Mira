# ── Never commit: model weights and caches (13 GB+) ──────────────────────────
*.npz
*.pb
*.onnx
*.safetensors
*.ckpt
sa3-studio/.hf/
sa3-studio/.uvcache/
models/*
!models/README.md

# ── Virtualenvs (path-specific, rebuild with uv) ─────────────────────────────
.venv/
**/.venv/
__pycache__/
*.py[cod]

# ── Vendored upstream repos (cloned, not authored here) ──────────────────────
sa3-studio/stable-audio-3/
sa3-studio/underfit/
vendor/
lab/conversion_sources/

# ── C++ build outputs ──────────────────────────────────────────────────────
build/
**/build/
*.a
*.o
*.dylib

# ── Runtime state, datasets, outputs ─────────────────────────────────────────
sa3-studio/datasets/
sa3-studio/loras/
sa3-studio/**/state/
output/
*.wav
*.mp3
*.flac
*.aif
*.aiff
!fixtures/*.wav

# ── Local analysis artefacts ─────────────────────────────────────────────────
*.sqlite
*.sqlite3
*.db
embeddings.npy

# ── macOS / editors ──────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/

# SA3 training artifacts — too large / not source
sa3-studio/*.zip
sa3-studio/Screenshot*.png

# generate.sh runtime log
sa3-studio/.generate.log
sa3-studio/stable-audio-3/optimized/mlx/loras/

# spike/06 embedded-python artifacts (regenerated by build.sh)
spike/06_embedded_python/pyruntime/
spike/06_embedded_python/pysite/
spike/06_embedded_python/build/

# Pre-encoded LoRA latents — regenerable from audio via prepare-lora.sh / the
# SA3 Generate window, and hundreds of MB per film.
sa3-studio/latents/
